import { JwtResponseInterface } from './jwt-response-interface';

describe('JwtResponseInterface', () => {
  it('should create an instance', () => {
    expect(new JwtResponseInterface()).toBeTruthy();
  });
});
